apiVersion: v1
kind: Service

metadata:
  name: react-service
  namespace: react

spec:
  type: NodePort

  selector:
    app: react-app

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
